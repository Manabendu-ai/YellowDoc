# Where the FastAPI backend is listening.
# Read server-side only, so the backend never has to allow browser origins.
YELLOWDOC_API_URL=http://localhost:8000

# How long a single conversion may take before the proxy gives up (ms).
# Layout analysis + OCR + LLM structuring on a long scan can take minutes.
YELLOWDOC_CONVERT_TIMEOUT_MS=600000
